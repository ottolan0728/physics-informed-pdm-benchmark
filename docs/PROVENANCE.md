# Provenance and reconciliation note

The repository package was reconciled against the final published article (Sensors 2026, 26(16), 5026), the public repository snapshot, and the available local development archive.

The archive contained an early course-oriented notebook and a script with settings that differed from Table 6. Those materials are not included in the public release. The canonical implementation uses the published configurations. Reported article values are retained in a separate immutable reference directory so that generated rerun results cannot silently overwrite them.

The final corrected Figures 3, 6, 7, and the supplied Figure 17 replace earlier repository images. The dataset, manuscript source, peer-review correspondence, local absolute paths, serialized models, and obsolete course notebooks are excluded.

